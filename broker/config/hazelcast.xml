<hazelcast
        xsi:schemaLocation="http://www.hazelcast.com/schema/config
  http://www.hazelcast.com/schema/config/hazelcast-config-3.5.xsd"
        xmlns="http://www.hazelcast.com/schema/config"
        xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance">
    <network>
        <join><multicast enabled="true"/></join>
    </network>

    <map name="messages_map">
    </map>

    <map name="groups_map">
    </map>

    <map name="users">
    </map>

    <multimap name="group_members">
    </multimap>

    <multimap name="user_groups">
    </multimap>

    <multimap name="user_message_ids">
    </multimap>

    <multimap name="chatroom_message_ids">
    </multimap>

    <multimap name="user_friends">
    </multimap>

    <multimap name="user_friends_request">
    </multimap>


</hazelcast>
